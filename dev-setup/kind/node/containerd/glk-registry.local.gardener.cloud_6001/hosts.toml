# Enable containerd to reach registry at glk-registry.local.gardener.cloud:6001 via HTTP.
# Beware: Port `:5000` cannot be used, as it conflicts with the default port allocation for the `AirPlay Receiver` on MacOS.
server = "http://glk-registry.local.gardener.cloud:6001"

[host."http://glk-registry.local.gardener.cloud:6001"]
  capabilities = ["pull", "resolve"]
